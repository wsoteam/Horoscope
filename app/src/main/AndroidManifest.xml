<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wsoteam.horoscopes">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_key" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />


        <activity android:name="com.wsoteam.horoscopes.SplashActivity"
            android:theme="@style/Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.form.FormActivity"
            android:theme="@style/Form"
            android:screenOrientation="portrait"/>
        <activity android:name=".presentation.premium.PremiumHostActivity"
            android:theme="@style/Premium"
            android:screenOrientation="portrait"/>
        <activity android:name=".MainActivity"
            android:theme="@style/Main"
            android:screenOrientation="portrait"/>

        <activity android:name=".presentation.settings.SettingsActivity"
            android:theme="@style/Main"
            android:screenOrientation="portrait"/>

        <activity android:name=".presentation.ball.BallActivity"
            android:screenOrientation="portrait"/>


        <service
            android:name=".notification.NotificationService"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <receiver android:name=".notification.AlarmReceiver" />

        <receiver android:name=".notification.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.HOME" />
            </intent-filter>
        </receiver>
    </application>

</manifest>